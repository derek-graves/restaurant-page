const contact = () => {
  // maintain or adjust .main bg-color
  const main = document.querySelector(".main");
  if (!main.classList.contains("show-main-bg")) {
    main.classList.add("show-main-bg");
  }

  // generate contents
};
